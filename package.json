{
  "name": "nutriscan-ai-root",
  "version": "1.0.0",
  "description": "Root configuration for launching NutriScan AI server and client systems.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "npx concurrently --kill-others-on-fail \"npm run dev --prefix server\" \"npm run dev --prefix client\""
  }
}
